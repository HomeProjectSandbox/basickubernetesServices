apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db
spec:
  selector:
    app: {{ .Release.Name }}-db
  ports:
    - protocol: TCP
      port: {{ .Values.db.ports[0].hostPort }}
      targetPort: {{ .Values.db.ports[0].containerPort }}