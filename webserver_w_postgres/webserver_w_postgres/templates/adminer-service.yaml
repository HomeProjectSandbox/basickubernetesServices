apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-adminer
spec:
  selector:
    app: {{ .Release.Name }}-adminer
  ports:
    - protocol: TCP
      port: {{ .Values.adminer.ports[0].hostPort }}
      targetPort: {{ .Values.adminer.ports[0].containerPort }}
  