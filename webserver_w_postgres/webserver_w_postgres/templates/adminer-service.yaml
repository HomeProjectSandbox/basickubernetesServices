apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-adminer
spec:
  selector:
    app: {{ .Release.Name }}-adminer
  ports:
    - protocol: TCP
      port: {{ .Values.adminer.ports.hostPort }}
      targetPort: {{ .Values.adminer.ports.containerPort }}
  